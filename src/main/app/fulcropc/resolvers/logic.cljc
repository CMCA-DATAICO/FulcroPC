(ns app.fulcropc.resolvers.logic
  "Pure business logic for matches, leagues, and tournaments.
   This namespace contains no database or resolver code — only pure functions
   that can be used from both client and server.")
